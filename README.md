# TUNE TOTS — быстрый редактируемый сайт

Это простая статическая версия сайта Tune Tots. Тексты лежат прямо в `index.html`, стили — в `styles.css`, интерактивы — в `content.js`.

## GitHub Pages

Сайт публикуется из ветки `main`, папка `/`.
